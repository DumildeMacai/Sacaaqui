import type { Atm } from '@/types';

// This file is no longer in use, but kept for reference if needed.
// The application now fetches data directly from Firestore.
export const mockAtms: Atm[] = [
];
